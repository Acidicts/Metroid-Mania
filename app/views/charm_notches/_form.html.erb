<%= form_with(model: charm_notch) do |form| %>
  <% if charm_notch.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(charm_notch.errors.count, "error") %> prohibited this charm_notch from being saved:</h2>

      <ul>
        <% charm_notch.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :charm_slot_id, style: "display: block" %>
    <%= form.text_field :charm_slot_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
