name: CI

on:
  pull_request:
  merge_group:
  push:
  workflow_dispatch: 

jobs:
  scan_ruby:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libblas-dev liblapack-dev libpq-dev build-essential

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: false

      - name: Install gems
        run: |
          # Allow updating Gemfile.lock in CI when Gemfile changes (avoids frozen failure)
          bundle config set frozen false || true
          bundle install --jobs 4

      - name: Scan for common Rails security vulnerabilities using static analysis
        run: bin/brakeman --no-pager --no-exit-on-warn

  scan_js:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libblas-dev liblapack-dev libpq-dev build-essential

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: false

      - name: Install gems
        run: |
          bundle config set frozen false || true
          bundle install --jobs 4


  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libblas-dev liblapack-dev libpq-dev build-essential

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: false

      - name: Install gems
        run: |
          bundle config set frozen false || true
          bundle install --jobs 4

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install JS dependencies
        run: |
          if [ -f package.json ]; then
            npm ci
          else
            echo "No package.json found — skipping npm install."
          fi

      - name: Lint code for consistent style
        run: bin/rubocop -f github
        # need to fix this? bin/rubocop -f github --autocorrect

      - name: Lint ERB templates
        run: bundle exec erb_lint --lint-all
        # need to fix this? bundle exec erb_lint --lint-all --autocorrect

      - name: Check JavaScript/TypeScript formatting
        run: |
          if [ -f package.json ]; then
            npx prettier --check "app/javascript/**/*.{js,jsx,ts,tsx}"
          else
            echo "No package.json — skipping JS formatting check."
          fi

      - name: Check SCSS/CSS formatting
        run: |
          if [ -f package.json ]; then
            npx prettier --check "app/assets/stylesheets/**/*.{scss,css}"
          else
            echo "No package.json — skipping SCSS/CSS formatting check."
          fi

  db_checks:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: flavortown_test
        ports:
          - 5432:5432
        options: --health-cmd="pg_isready" --health-interval=10s --health-timeout=5s --health-retries=3
    env:
      RAILS_ENV: test
      DATABASE_URL: postgresql://postgres:password@localhost:5432/flavortown_test
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libblas-dev liblapack-dev libpq-dev build-essential
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: false

      - name: Install gems
        run: |
          bundle config set frozen false || true
          bundle install --jobs 4

      - name: Set required env vars for Rails
        run: |
          # Use secrets when available, otherwise provide safe dummy values so initializers don't crash
          if [ -z "${{ secrets.HACKCLUB_CLIENT_ID }}" ]; then
            echo "HACKCLUB_CLIENT_ID=dummy" >> $GITHUB_ENV
          else
            echo "HACKCLUB_CLIENT_ID=${{ secrets.HACKCLUB_CLIENT_ID }}" >> $GITHUB_ENV
          fi
          if [ -z "${{ secrets.HACKCLUB_CLIENT_SECRET }}" ]; then
            echo "HACKCLUB_CLIENT_SECRET=dummy" >> $GITHUB_ENV
          else
            echo "HACKCLUB_CLIENT_SECRET=${{ secrets.HACKCLUB_CLIENT_SECRET }}" >> $GITHUB_ENV
          fi

      - name: Check schema matches migrations
        id: migration_check
        run: |
          bundle exec rails db:migrate
          git diff --exit-code db/schema.rb
      - name: Comment on schema failure
        if: failure() && steps.migration_check.outcome == 'failure' && github.event_name == 'pull_request'
        uses: actions/github-script@v8
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `### :rage2: you GOOF!\n Schema dump has fallen out of date\n\nRun \`rails db:migrate\` locally and commit the changes to \`db/schema.rb\`.`
            });

      - name: Check annotations are up to date
        id: annotate_check
        if: success()
        run: bundle exec annotaterb models --frozen
      - name: Comment on annotation failure
        if: failure() && steps.annotate_check.outcome == 'failure' && github.event_name == 'pull_request'
        uses: actions/github-script@v8
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `### :rage1: you SILLY GOOSE!\nModel annotations have fallen out of date.\n\nPlease run \`bundle exec annotaterb models\` locally and commit the changes.`
            });

  # test:
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: postgres:15
  #       env:
  #         POSTGRES_USER: postgres
  #         POSTGRES_PASSWORD: postgres
  #         POSTGRES_DB: flavortown_test
  #       ports:
  #         - 5432:5432
  #       options: --health-cmd="pg_isready" --health-interval=10s --health-timeout=5s --health-retries=3
  #   steps:
  #     - name: Install packages
  #       run: sudo apt-get update && sudo apt-get install --no-install-recommends -y build-essential git libyaml-dev pkg-config google-chrome-stable

  #     - name: Checkout code
  #       uses: actions/checkout@v6

  #     - name: Set up Ruby
  #       uses: ruby/setup-ruby@v1
  #       with:
  #         ruby-version: .ruby-version
  #         bundler-cache: true

  #     - name: Run tests
  #       env:
  #         RAILS_ENV: test
  #         DATABASE_URL: postgresql://postgres:postgres@localhost:5432/flavortown_test
  #         # REDIS_URL: redis://localhost:6379/0
  #       run: bin/rails db:test:prepare test test:system

  #     - name: Keep screenshots from failed system tests
  #       uses: actions/upload-artifact@v5
  #       if: failure()
  #       with:
  #         name: screenshots
  #         path: ${{ github.workspace }}/tmp/screenshots
  #         if-no-files-found: ignore

